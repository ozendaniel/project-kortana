# DoorDash restaurant menu query
# TODO: Capture actual query from DevTools
#
# Steps to capture:
# 1. Open doordash.com with DevTools Network tab (filter: graphql)
# 2. Navigate to a restaurant page
# 3. Copy the storepageFeed query from the request payload
#
# Expected variables: { storeId }
# Expected response: menu categories with items, prices, modifiers
